// Simple arrow function that adds two numbers
val addNumbers = (x, y) => { x + y; };

// Arrow function that increments a number by 1
val incrementByOne = (x) => {
    return x + 1;
};

// Arrow function that adds two numbers with explicit return
val addTwoNumbers = (x, y) => {
    return x + y;
};

println("Testing arrow function execution");
println(incrementByOne(1));