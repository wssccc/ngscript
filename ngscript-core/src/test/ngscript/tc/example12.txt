// Define a callback function that takes a parameter
val callbackFunction = function(inputValue) {
    println("Executing callback with value: " + inputValue);
};

// Execute the callback function with a test value
go callbackFunction(123);
println("Main routine execution completed");